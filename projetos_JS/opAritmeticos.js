const [a, b, c, d] = [3, 5, 1, 15]

const soma = a + b + c + b

const subtracao = d - b 

const multiplicacao = a * b

const divisao = d / a
// serve para saber se o numero é impar/par
const modulo = a % 2

console.log( soma, subtracao, multiplicacao, divisao, modulo)